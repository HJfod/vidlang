
function length(list: [any]) -> int {
    invoke_intrinsic list_length(list)
}

function add(list: [any], item: (typeof list)::Item) {
    invoke_intrinsic list_add(list, item)
}
function remove(list: [any], item: (typeof list)::Item) {
    invoke_intrinsic list_remove(list, item)
}
