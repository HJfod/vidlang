
effect blur(target: Clip, size: float) {
    let original_render = target.render;
    target.render = canvas -> {
        let buffer = canvas.copy();
        original_render(buffer);
        // todo: impl gaussian blur
    };
}
